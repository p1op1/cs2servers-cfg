// Переменные

```javascript
  private $match_id;
  private $players = [];
  private $server_ip;
  private $password;
  private $password;
  private $rconPassword;
  private $score = ["team_a" => 0, "team_b" => 0];
  private $config_kniferound = true;
  private $config_streamer = false;
  private $streamerReady = false;
  private $teamAName;
  private $teamBName;
  private $teamAFlag = none;
  private $teamBFlag = none;
  private $isPaused;
  private $timeEngageMap = 0;
  private $mapIsEngaged = false;
  private $waitRoundStartRecord = false;
  private $forceRoundStartRecord = false;
  private $delay_ready_inprogress = false;
  private $delay_ready_countdown = 10;
```

// Server Defaults

hostname "test_servak"                // Set server hostname
sv_cheats 0                   // Enable or disable cheats
sv_hibernate_when_empty 0  // Disable server hibernation

// Passwords

rcon_password "changeme"            // Set rcon password
sv_password "changeme"                   // Set server password
rcon_connected_clients_allow true

 
